# Generic ignores for all projects
.DS_Store
Thumbs.db
ehthumbs.db
Desktop.ini
*.log
logs/
*.txt
*.swp
*.bak
*.orig
*.obj
*.o
*.rej
*~
.#*
.#*#

# Maven
# Ignore the entire target directory where compiled code and packages are built
/target/
pom.xml.tag
pom.xml.releaseBackup
pom.xml.versionsBackup
pom.xml.next
release.properties
dependency-reduced-pom.xml
buildNumber.properties
# Ignore the Maven Wrapper JAR as it's typically downloaded
.mvn/wrapper/maven-wrapper.jar

# Gradle (Include if you might ever use Gradle or have mixed projects)
# Ignore the entire build directory for Gradle projects
/build/
.gradle/

# Compiled class files and packaged artifacts
*.class
*.jar
*.war
*.ear
*.zip
*.tar.gz
*.rar

# IntelliJ IDEA specific files
.idea/
*.iml
*.iws
*.ipr
out/ # IntelliJ's default output directory

# Eclipse / STS specific files
.apt_generated/
.classpath
.factorypath
.project
.settings/
.springBeans
.sts4-cache/

# NetBeans specific files
/nbproject/private/
/nbbuild/
/dist/
/nbdist/
/.nb-gradle/

# VS Code specific files
.vscode/

# Environment variables and sensitive local configurations
.env
.env.*
*.env
# AWS specific (if you generate credentials locally)
.aws/credentials

# Docker related files (for the backend project itself)
# Note: docker-compose.yml is usually in the parent, but if you had
# a Dockerfile specific to *this* module, its logs/tmp files are ignored.
*.log
*.tmp
.dockerignore # Often contains similar entries, but good to include here too

# Spring Boot specific local configuration files
# These are often used for local development and should not be committed
/src/main/resources/application-local.properties
/src/main/resources/application-dev.properties
/src/main/resources/application*.yml.bak
/src/main/resources/application*.properties.bak

# Optional: If you have a HELP.md that is generated or purely local, ignore it
# HELP.md
